apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-mongo
  template:
    metadata:
      labels:
        app: node-mongo
    spec:
      containers:
        - name: node-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: node-mongo-srv
spec:
  selector:
    app: node-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017 # mongoのデフォルトポート
