name: test-client

on:
  pull_request:
    # 以下のディレクトリが変更されたPRのみ、このテストを実行
    paths:
      - "client/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd client && npm install && npm run test:ci
